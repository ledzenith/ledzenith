const ProductSection = () => {
    // Sezione in cui è presente il prodotto del "Carosello"(inizialmente prodotto principale)
    // La Sezione deve contenere delle immagini del prodotto(slider immagini), bottone Acquista,
    // Titolo del prodotto, Recensioni, Dettagli tecnici, etc...
    // Questa sezione cambia al click nel carosello

    return ( 
        pass
     );
}
 
export default ProductSection;